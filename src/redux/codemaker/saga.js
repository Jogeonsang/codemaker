import { takeEvery, put, call } from 'redux-saga/effects';

import * as service from '../../api/codemaker';
import {
    SUBMIT_DATA, submitData
} from './action';

export function* submitData(action) {
    const { }
}